// Placeholder for realtime sync (e.g., Socket.io, Supabase, Partykit)
import { useEffect } from 'react'

export function useRealtime(roomId) {
  useEffect(() => {
    // TODO: connect to server and sync
    return () => {
      // cleanup
    }
  }, [roomId])
}
